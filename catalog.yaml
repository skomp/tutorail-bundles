# Catalogue of the bundles in this repository.
#
# GENERATED by tutorail-authoring's catalog.py. Edit the bundles' tutorial.yaml
# files and run it again rather than editing this file, which is overwritten.
#
# Every source.path is RELATIVE to this file's own directory, which is the rule
# for every catalogue: a catalogue's bundles are resolved from the directory
# that holds it. That is what lets this repository be added as a catalogue and
# serve its own bundles.
#
# `scope` has no field in a bundle's tutorial.yaml. It is derived from the
# length of that bundle's lessons list.

catalog_version: 1
tutorials:
  - id: durable-event-broker
    title: Build a Durable Event Broker in Go
    description: >
      Build a persistent, partitioned event broker from first principles, then add
      batching, retention, observability, and a deliberately limited follower.
    subjects: [go, event-streaming, storage, distributed-systems]
    aliases: [message-broker, append-only-log, kafka-internals, event-log]
    level: intermediate-to-advanced
    style: [project-driven, interactive, long-form, measurement-led]
    scope: "15 lessons; a few weeks"
    workspace_kind: new-repository
    source:
      type: local
      path: durable-event-broker
  - id: rust-automaton-db
    title: Learn Rust by Building AutomatonDB
    description: >
      Project-driven Rust taught by building a serious automaton-native, partitioned
      database with storage-engine and distributed-systems depth.
    subjects: [rust, databases, distributed-systems, automata, storage-engines]
    aliases: [cassandra-like, key-value-store, kv-store, database-internals, systems-programming]
    level: intermediate-to-advanced
    style: [project-driven, interactive, systems-oriented, long-form]
    scope: "23 lessons; months of work"
    workspace_kind: existing-or-new-repository
    source:
      type: local
      path: rust-automaton-db
  - id: webgl-typescript-scene
    title: Learn WebGL 2 by Building a 3D Scene
    description: >
      Learn WebGL 2 from the rendering pipeline upward by building an interactive, lit
      and textured 3D scene in TypeScript, culminating in a loaded glTF model.
    subjects: [webgl2, computer-graphics, typescript, glsl, gltf]
    aliases: [webgl, graphics-programming, 3d-rendering, shaders]
    level: intermediate
    style: [project-driven, interactive, exercise-based]
    scope: "19 lessons; months of work"
    workspace_kind: new-repository
    source:
      type: local
      path: webgl-typescript-scene
